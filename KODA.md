# KODA.md - Инструкция по проекту Picast Laravel

## 📋 Обзор проекта

**Picast Laravel** — это комплексная веб-система управления доменами и YouTube видео, построенная на Laravel Framework с использованием принципов Domain-Driven Design (DDD). Проект реализует полноценную экосистему для отслеживания доменов, обработки видео контента и управления пользователями с ролевой системой доступа.

### 🎯 Основные возможности

- **🌐 Управление доменами**: Полный CRUD для доменов с автоматическим отслеживанием WHOIS данных
- **👥 Система ролей**: Гибкая система прав доступа на базе Spatie Laravel Permission
- **📧 Приглашения пользователей**: Безопасная система онбординга через коды приглашений
- **📅 Мониторинг доменов**: Автоматические уведомления о приближающихся истечениях доменов
- **🎥 Обработка YouTube**: Скачивание и управление YouTube видео с обработкой очередей
- **📱 Telegram уведомления**: Кастомный канал уведомлений для важных событий
- **🔗 REST API**: Комплексный API для внешних интеграций и автоматизации
- **🤖 ChadGPT интеграция**: Система чат-ботов с хранением истории и статистикой слов

### 🏗️ Архитектура проекта

Проект реализован по принципам **Domain-Driven Design (DDD)** с четким разделением на контексты:

```
app/Context/
├── Domains/          # Контекст управления доменами
│   ├── Domain/       # Бизнес-модели и сущности
│   ├── Application/  # Сервисы, политики и контракты
│   └── Infrastructure/ # Контроллеры, задачи и внешние интеграции
├── User/             # Контекст управления пользователями
│   ├── Domain/       # Пользовательские модели
│   └── Infrastructure/ # Пользовательские контроллеры и сервисы
├── Youtube/          # Контекст обработки YouTube
│   ├── Domain/       # Видео модели и сущности
│   ├── Application/  # Сервисы обработки видео
│   └── Infrastructure/ # Видео контроллеры и задачи
└── ChadGPT/          # Контекст чат-бот функций
    ├── Domain/       # Модели разговоров и статистики
    └── Infrastructure/ # Контроллеры и сервисы чатов
```

## 🛠️ Технологический стек

### Backend технологии
- **Framework**: Laravel 12.x
- **PHP**: 8.4+ (последняя стабильная версия)
- **База данных**: MySQL 8.3
- **Кэширование**: Memcached
- **Очереди**: Laravel Queue с Supervisor
- **Аутентификация**: Laravel Sanctum для API токенов

### Frontend технологии
- **Сборщик**: Vite 7.0+
- **CSS Framework**: Tailwind CSS 4.0
- **JavaScript**: Axios для HTTP запросов
- **UI**: Laravel Blade шаблоны

### Ключевые зависимости PHP
- `spatie/laravel-permission` - управление ролями и правами
- `io-developer/php-whois` - получение WHOIS данных
- `norkunas/youtube-dl-php` - скачивание YouTube видео
- `alaouy/youtube` - интеграция с YouTube API
- `irazasyed/telegram-bot-sdk` - функциональность Telegram ботов
- `intervention/image` - обработка изображений
- `guzzlehttp/guzzle` - HTTP клиент
- `knuckleswtf/scribe` - генератор API документации

### Инструменты разработки
- `larastan/larastan` - PHPStan для Laravel-специфичного анализа
- `laravel/pint` - исправление стиля кода PSR-12
- `barryvdh/laravel-debugbar` - панель отладки
- `fakerphp/faker` - генерация тестовых данных

### Инфраструктура и DevOps
- **Контейнеризация**: Docker & Docker Compose
- **Веб-сервер**: Nginx 1.17
- **Менеджер процессов**: Supervisor для воркеров очередей
- **Тестирование email**: MailHog для разработки
- **Администрирование БД**: Adminer
- **Отладка**: XDebug с интеграцией IDE

## 🐳 Локальная разработка

### Предварительные требования
- Docker & Docker Compose
- Make (для удобных команд)
- Git

### Быстрый старт

1. **Клонирование репозитория**:
   ```bash
   git clone https://github.com/Simtel/picast-laravel.git
   cd picast-laravel
   ```

2. **Сборка Docker контейнеров**:
   ```bash
   make build
   ```

3. **Запуск среды разработки**:
   ```bash
   make up
   ```

4. **Установка PHP зависимостей**:
   ```bash
   make composer-install
   ```

5. **Создание конфигурации окружения**:
   ```bash
   cp .env.example .env
   ```
   
   Редактирование файла `.env` с вашей конфигурацией:
   ```env
   DB_CONNECTION=mysql
   DB_HOST=db
   DB_PORT=3306
   DB_DATABASE=picast
   DB_USERNAME=root
   DB_PASSWORD=example
   
   YOUTUBE_API_KEY=your_youtube_api_key
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token
   ```

6. **Создание баз данных**:
   - Доступ к Adminer по адресу [http://localhost:8080](http://localhost:8080)
   - Вход: Server: `db`, Username: `root`, Password: `example`
   - Создать базы данных: `picast` и `picast_test`

7. **Выполнение миграций базы данных**:
   ```bash
   make migrate
   ```

8. **Заполнение базы данных начальными данными**:
   ```bash
   make seed
   ```

9. **Установка и сборка frontend ресурсов**:
   ```bash
   npm install
   npm run build
   ```

### Сервисы разработки

После запуска будут доступны следующие сервисы:

| Сервис | URL | Описание |
|--------|-----|----------|
| Приложение | [http://localhost](http://localhost) | Основное приложение |
| Adminer | [http://localhost:8080](http://localhost:8080) | Администрирование БД |
| MailHog | [http://localhost:8025](http://localhost:8025) | Интерфейс тестирования email |
| API Документация | [http://localhost/docs](http://localhost/docs) | Интерактивная API документация |

## 📋 Доступные Make команды

```bash
make help              # Показать все доступные команды
make up                # Запустить все контейнеры
make down              # Остановить контейнеры
make restart           # Перезапустить контейнеры
make cli               # Доступ к shell PHP контейнера
make mysql-console     # Доступ к MySQL консоли
make migrate           # Выполнить миграции БД
make seed              # Заполнить БД тестовыми данными
make test              # Запустить PHPUnit тесты
make test-coverage     # Сгенерировать отчет о покрытии тестами
make phpstan           # Запустить статический анализ
make pint              # Исправить стиль кода
make worker            # Запустить воркер очереди
make composer-install  # Установить пакеты Composer
make phpstan           # Запустить PHPStan анализ
make set-githooks      # Установить git hooks
```

## 📡 API документация

Приложение предоставляет комплексный REST API для автоматизации и внешних интеграций.

### Базовый URL
```
https://your-domain.com/api/v1
```

### Аутентификация
API использует токен-базовую аутентификацию. Включите токен в заголовок Authorization:
```
Authorization: Bearer YOUR_API_TOKEN
```

### Основные endpoints

#### Управление пользователями
```http
GET    /api/v1/user/current    # Получить текущего аутентифицированного пользователя
```

#### Управление доменами
```http
GET    /api/v1/domains         # Список всех доменов пользователя
POST   /api/v1/domains         # Создать новый домен
GET    /api/v1/domains/{id}    # Получить детали домена с WHOIS
PUT    /api/v1/domains/{id}    # Обновить домен
DELETE /api/v1/domains/{id}    # Удалить домен
```

#### Управление YouTube видео
```http
GET    /api/v1/video           # Список видео пользователя
POST   /api/v1/video           # Поставить видео в очередь на скачивание
GET    /api/v1/video/{id}      # Получить детали видео
PUT    /api/v1/video/{id}      # Обновить видео
DELETE /api/v1/video/{id}      # Удалить видео
```

### Примеры запросов

#### Создание домена
```bash
curl -X POST https://your-domain.com/api/v1/domains \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "example.com"}'
```

#### Постановка YouTube видео в очередь
```bash
curl -X POST https://your-domain.com/api/v1/video \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://youtube.com/watch?v=VIDEO_ID"}'
```

### Формат ответа
Все API ответы следуют единому JSON формату:

```json
{
  "data": {
    "id": 1,
    "name": "example.com",
    "created_at": "2024-01-01T00:00:00Z",
    "updated_at": "2024-01-01T00:00:00Z"
  },
  "meta": {
    "pagination": {
      "current_page": 1,
      "total": 50
    }
  }
}
```

### Обработка ошибок
API ошибки возвращают соответствующие HTTP коды с детальными сообщениями об ошибках:

```json
{
  "message": "Validation failed",
  "errors": {
    "name": ["The name field is required."]
  }
}
```

### Интерактивная документация
Для подробной API документации с интерактивными примерами посетите:
- **Локально**: [http://localhost/docs](http://localhost/docs)
- **OpenAPI спецификация**: [http://localhost/docs/openapi.yaml](http://localhost/docs/openapi.yaml)

## 🧪 Тестирование

### Запуск тестов
```bash
# Запустить все тесты
make test

# Запустить тесты с покрытием
make test-coverage

# Запустить конкретный тест
docker exec -it picast_php php artisan test --filter DomainTest
```

### Структура тестов
- **Feature Tests**: `/tests/Feature/` - Интеграционные тесты для полных рабочих процессов
- **Unit Tests**: `/tests/Unit/` - Изолированные модульные тесты для конкретных компонентов
- **API Tests**: `/tests/Feature/Api/` - Тесты API endpoints

## 🔧 Качество кода

### Статический анализ
```bash
# Запустить PHPStan анализ
make phpstan

# Исправить стиль кода
make pint
```

### Pre-commit хуки
Настройка Git хуков для автоматической проверки качества кода:
```bash
make set-githooks
```

## 📦 Развертывание

### Требования для продакшена
- PHP 8.3+
- MySQL 8.0+
- Nginx или Apache
- Supervisor для воркеров очередей
- SSL сертификат для HTTPS

### Конфигурация окружения
Ключевые переменные окружения для продакшена:

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com

# База данных
DB_CONNECTION=mysql
DB_HOST=your-db-host
DB_DATABASE=picast
DB_USERNAME=your-db-user
DB_PASSWORD=your-secure-password

# Внешние API
YOUTUBE_API_KEY=your-youtube-api-key
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Очереди
QUEUE_CONNECTION=database

# Кэширование
CACHE_DRIVER=memcached
MEMCACHED_HOST=127.0.0.1
```

### Шаги развертывания
1. Клонировать репозиторий на продакшен сервер
2. Установить PHP зависимости: `composer install --no-dev --optimize-autoloader`
3. Настроить переменные окружения
4. Выполнить миграции: `php artisan migrate --force`
5. Собрать ресурсы: `npm run build`
6. Настроить воркеры очередей с Supervisor
7. Настроить веб-сервер (Nginx/Apache)
8. Настроить SSL сертификат

## 🤝 Участие в разработке

1. Форкнуть репозиторий
2. Создать ветку для фичи: `git checkout -b feature/new-feature`
3. Внести изменения, следуя стандартам PSR-12
4. Запустить тесты: `make test`
5. Запустить статический анализ: `make phpstan`
6. Исправить стиль кода: `make pint`
7. Зафиксировать изменения: `git commit -am 'Add new feature'`
8. Отправить в ветку: `git push origin feature/new-feature`
9. Создать Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License.

## 🆘 Поддержка

Для вопросов и проблем:
- Создать issue на GitHub
- Проверить [API документацию](http://localhost/docs) для вопросов по API
- Изучить тестовые файлы для примеров использования

---

**Примечание**: Данный файл является живым документом и должен обновляться при изменении архитектуры или функциональности проекта.